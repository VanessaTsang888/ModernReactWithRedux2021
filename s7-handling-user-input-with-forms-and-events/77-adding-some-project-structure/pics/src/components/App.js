// This App.js component file is instead of defining App component insdie of index.js file and needs to be located
// inside the src -> components directories.

import React from 'react';

// Mkae a functional component that will return a div of App.
const App = () => {
  return <div>App</div>;
};

// Return the App so other files can use this component.
export default App;
